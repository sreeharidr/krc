# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Node / build artifacts (none today, but safe defaults)
node_modules/
dist/
build/
.cache/

# Environment / secrets (we don't have any, but never commit these)
.env
.env.local
*.key
*.pem

# Logs
*.log
npm-debug.log*

# OS thumbnails
Thumbs.db
ehthumbs.db

# Agent tooling — local only, never deployed.
# Everything in the repo root is uploaded as a static asset by wrangler, so
# keeping these untracked also keeps them off the public domain.
# Reinstall with:
#   npx skills add https://github.com/anthropics/skills --skill frontend-design
.agents/
.claude/
skills-lock.json

# Throwaway design mockups (not part of the site)
mockup-*.html
